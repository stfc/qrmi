---
# Workflow that builds Sphinx documentation and deploys it to GitHub Pages.

name: Sphinx Documentation

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - 'python/qrmi/**'
      - 'src/**'
      - '**/*.h'
      - 'Cargo.toml'
      - 'pyproject.toml'
      - 'Doxyfile'

  pull_request:  
    branches:
    - main
    paths:
      - 'docs/**'
      - 'python/qrmi/**'
      - 'src/**'
      - '**/*.h'
      - 'Cargo.toml'
      - 'pyproject.toml'
      - 'Doxyfile'

  workflow_dispatch:

  release:
    type:
      - published

  merge_group:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: |
            requirements-dev.txt
            pyproject.toml

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[all, docs]"

      - name: Build Python API documentation
        run: |
          sphinx-apidoc --no-toc -o docs/_api/python/ python/qrmi/

      - name: Verify Python API documentation
        run: |
          test "$(find docs/_api/python -name '*.rst' | wc -l)" -gt 0

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust artifacts
        uses: Swatinem/rust-cache@v2

      - name: Build Rust API documentation
        run: |
          cargo doc --no-deps

      - name: Verify Rust API documentation
        run: |
          test -f target/doc/qrmi/index.html

      - name: Install Doxygen
        run: |
          sudo apt-get update
          sudo apt-get install -y doxygen

      - name: Generate Doxygen HTML
        run: |
          mkdir -p build/doxygen
          doxygen Doxyfile

      - name: Verify Doxygen HTML
        run: |
          test -s build/doxygen/html/index.html

      - name: Cache Sphinx doctrees
        uses: actions/cache@v4
        with:
          path: docs/_build/.doctrees
          key: sphinx-${{runner.os}}-${{hashFiles('docs/**/*.rst', 'docs/**/*.md', 'docs/conf.py', 'python/**/*.py')}}
          restore-keys: |
            sphinx-${{runner.os}}-

      - name: Build Sphinx documentation
        run: |
          sphinx-build \
            --keep-going \
            -b html \
            -j auto \
            docs docs/_build/html

      - name: Check Sphinx documentation links
        continue-on-error: true
        run: |
          sphinx-build \
            -W \
            -b linkcheck \
            docs docs/_build/linkcheck

      - name: Add Rust API documentation to Sphinx
        run: |
          mkdir -p docs/_build/html/rust
          cp -R target/doc/* docs/_build/html/rust/

      - name: Add C API documentation to Sphinx
        run: |
          mkdir -p docs/_build/html/c
          cp -R build/doxygen/html/* docs/_build/html/c/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/_build/html

      - name: Upload linkcheck report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: linkcheck-report
          path: docs/_build/linkcheck/

  deploy:
    if: github.event_name == 'push' &&
        github.ref == 'refs/heads/main'

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

