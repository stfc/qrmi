[build-system]
requires = ["maturin>=1.9.4,<2.0"]
build-backend = "maturin"

[project]
name = "qrmi"
authors = [
  {email = "qiskit@us.ibm.com"},
  {name = "Alice and Bob, IBM, Pasqal SAS and UKRI-STFC (Hartree Centre)"}
]
requires-python = ">=3.11"
classifiers = [
    "Programming Language :: C",
    "Programming Language :: Rust",
    "Programming Language :: Python :: Implementation :: CPython",
    "Programming Language :: Python :: Implementation :: PyPy",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS",
    "Topic :: Software Development :: Libraries",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Scientific/Engineering :: Physics",
]
dynamic = ["version"]
license = "Apache-2.0"
license-files = ["LICENSE.txt"]
readme = "README.md"
description = "Quantum Resource Management Interface(QRMI)"
description_content_type = "text/markdown"
keywords = [
    "quantum-computing",
    "quantum-programming-language",
    "qiskit",
    "pulser",
    "slurm",
    "slurm-job-scheduler",
    "slurm-workload-manager",
    "python",
    "rust",
]
dependencies = [
  "python-dotenv",
  "python-dateutil",
]

[project.optional-dependencies]
ibm = [
  "qiskit_ibm_runtime>=0.30.0",
  "qiskit_qasm3_import",
]
pasqal = [
  "qiskit-pasqal-provider>=0.1.1",
  "pulser>=1.9.0",
]
iqm = [
  "iqm-client[qiskit]",
]
alice-bob = [
  "qiskit-alice-bob-provider",
]
docs = [
  "sphinx>=9.1.0",
  "sphinx-autobuild>=2025.08.25",
  "shibuya>=2026.5.19",
  "sphinx_design>=0.7.0",
  "sphinx-tabs>=3.5.0",
  "sphinx_contributors>=0.3.0",
  "sphinx-copybutton>=0.5.2",
  "sphinx-togglebutton>=0.4.5",
]
# 'iqm' should be removed, but can't for now - will impact to test-wheels in Makefile.
all = ["qrmi[ibm,pasqal,iqm]"]

[project.urls]
Repository = "https://github.com/qiskit-community/qrmi"
Homepage = "https://qiskit-community.github.io/qrmi/"
Documentation = "https://qiskit-community.github.io/qrmi/"
"API Reference" = "https://qiskit-community.github.io/qrmi/development/api_references/index.html"
Issues = "https://github.com/qiskit-community/qrmi/issues"

[tool.maturin]
python-source = "python"
module-name   = "qrmi._core" 
features = ["pyo3/abi3", "qrmi/pyo3"]
python-packages = [
    "qrmi",
]
include = [
  "python/qrmi/py.typed",
  "python/qrmi/*.pyi",      
]

[project.scripts]
task_runner = "qrmi.tools.task_runner.main:run"

[tool.cibuildwheel]
manylinux-x86_64-image = "manylinux_2_28"
manylinux-ppc64le-image = "manylinux_2_28"
manylinux-aarch64-image = "manylinux_2_28"

[tool.cibuildwheel.linux]
before-all = 'curl -sSf https://sh.rustup.rs | sh -s -- -y'
before-test = 'pip install -r requirements-dev.txt'
environment = { PATH='$HOME/.cargo/bin:$PATH' }
skip = '*musllinux* cp*t-*'
test-extras = ["ibm", "pasqal"]
test-command = [
  "pytest {project}/python/tests/unit",
  "VENV_IQM=$(mktemp -d)",
  "python -m venv $VENV_IQM",
  "$VENV_IQM/bin/pip install -r {project}/requirements-dev.txt '{wheel}[iqm]'",
  "$VENV_IQM/bin/pytest {project}/python/tests/compatibility",
]

# Run only the unit tests where [iqm] cannot be installed:
# - aarch64, Python 3.14: qiskit-aer (required by iqm-client[qiskit]) has no
#   wheels yet, see https://github.com/Qiskit/qiskit-aer/issues/2419
# - ppc64le: grpcio (required by iqm-station-control-client) has no wheels,
#   and building it from source fails
[[tool.cibuildwheel.overrides]]
select = "cp314-manylinux_aarch64 *-manylinux_ppc64le"
test-command = ["pytest {project}/python/tests/unit"]
